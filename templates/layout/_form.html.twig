{{ form_start(form) }}

{{ form_widget(form) }}

{% set has_required_fields = false %}
{% for child in form %}
    {% if child.vars.required is defined and child.vars.required %}
        {% set has_required_fields = true %}
    {% endif %}
{% endfor %}


{{ form_end(form) }}