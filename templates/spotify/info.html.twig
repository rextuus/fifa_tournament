{% extends 'base.html.twig' %}

{% block title %}Spotify Song Suche{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="text-center">Spotify Verwaltung</h1>
    </div>

    <!-- Render the Search Form -->
    <div class="container my-5">

    <div class="row justify-content-center mt-4">
        Eingeloggt als:
        {{ displayName }}
        <div

        {% if displayName == 'not_logged_in' %}
            <div class="row justify-content-center mt-4">
                <a href="{{ path('spotify_login') }}">Login</a>
            </div>
        {% endif %}
    </div>

{% endblock %}