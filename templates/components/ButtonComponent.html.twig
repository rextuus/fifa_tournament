<div{{ attributes }}>
    <form method="{{ this.method }}" action="{{ this.route }}" style="display: inline;">
        <button type="submit" class="{{ this.classes }}">
            {{ this.text }}
        </button>
    </form>
</div>
